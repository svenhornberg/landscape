// Lokaler Webserver. Noch nicht gebaut, siehe SPEC.md Schritt 3.

export function starte(_wurzel: string, _port: number, _open: boolean): number {
  console.error("bplan serve: noch nicht implementiert (Schritt 3 in SPEC.md)");
  return 1;
}
